# specs — 機能ごとの要件と設計

機能 1 つにつきフォルダ 1 つ。

```
docs/specs/<機能名>/
  requirements.md   誰が何をできるようになるか。受け入れ条件
  design.md         データの形 (AppData の data の中身)、画面、CPOS のどの API を使うか
```

小さな機能なら 1 ファイルでもよい。書かずに作り始めないこと (AI にも同じ)。
