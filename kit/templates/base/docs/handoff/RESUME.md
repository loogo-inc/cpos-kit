# RESUME — 次のセッションへ

作業を始めるときに読む。終わるときに書き直す。長くしない (30 行まで)。

## いまの状態

- cpos-kit で生成した直後。KIT 模擬サーバで `npm run dev` と `npm test` が動く。

## 次にやること

1. `docs/PRODUCT.md` に何を作るかを書く
2. 最初の機能を `docs/specs/<機能名>/` に書く

## 引っかかっていること

- (なし)

(このファイルは上書き型で、やり残しを落としうる。作業が複数セッションにまたがるなら `npx cpos-kit tickets init` でチケット台帳を入れ、`docs/TICKETS.md` を正本にする)
