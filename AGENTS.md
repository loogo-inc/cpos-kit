# AGENTS.md — このリポジトリで作業するエージェントへ

このリポジトリは **CPOS の上でアプリを作る人に配る共通開発基盤** の置き場である。
ここにプロダクトのコードはない。あるのは配るもの (パッケージ・雛形・skills) と、そのテストである。

## 何があるか

- `spec/` — 正本。`cpos-openapi.json` (CPOS の公式 OpenAPI の写し。client の生成元)、manifest の JSON Schema、模擬サーバの契約 (`cpos-api.yaml`)。
- `kit/` — 配るもの。client (手書き 5 系統 + 生成された全 operation `api.json` / `api.d.ts`) / manifest / fake / testing / app-kit、標準ブロック、雛形、CLI、テスト。
- `skills/cpos/` — 配るスキル (Agent Skills 形式)。
- `kit/api.json` `kit/api.d.ts` `spec/cpos-openapi.json` は生成物。手で直さない (保守者が公式 OpenAPI から再生成する)。

<!-- cpos-kit:discipline:begin (この間は cpos-kit が所有する。手で直さない。外すなら節ごと消す) -->
## 作業規律: 止まる前に証拠

ターンを終える前に問う: 「自分で確かめられる・やれることが、まだ残っていないか」。残っていればやってから止まる。

- 聞かない: 自分でできる動作 (確かめる・試す・直す・進める) の許可は求めない。
- 丸めない: 「確認済み」「正常動作」「✅」は、自分で動かした出力を貼れるものだけ。動かしていないなら「未確認」と書き、書く前にやる。
- 他責しない: 「所有者の判断が要る」「◯◯側の問題」と書く前に自分のコードと数字を疑い、確かめた痕跡を添える。数字は出す前に相互に照合する。
- 予告しない: 「次は〜します」「いまやります」は、言わずにやる。

止めてよいのは、自分では作れない証拠が要るときだけ: (a) 無い情報 (b) 破壊的・外部公開の操作 (コミット・push・マージ・削除・デプロイ・送信) の承認 (c) 外部待ち。
そのときも、それに依存しない残りを全部やってから、1 つだけ聞く。
報告は「項目ごとに ✅/❌ ＋ 痕跡 ＋ 残数」。
<!-- cpos-kit:discipline:end -->

## 原則

- **CPOS の API は全部 client のメソッドにする** (公式 OpenAPI から機械生成。人が選ばない)。CPOS が増やせば再生成で増え、消せば消える。増減は CHANGELOG に機械的に書く。
- **`spec/cpos-api.yaml` は模擬サーバの契約。** 模擬サーバに応答を足すときだけ、本物で確かめた形を書く。
- **v1 内は追加のみ。** 手書きの系統 (masterUsers 等) の削除は `deprecated` と CHANGELOG。
- **docs を書かない。** 書くなら動くレシピ (コード + テスト) か生成物。
- **CPOS 本体の内部構造 (ファイル名・行番号・admin 専用の内訳)、ステージングの URL、トークンをこのリポジトリに書かない。** `kit/test/no-internal-leak.test.mjs` が検査する。
- **既存アプリのリポジトリに直接 PR しない。**
- スクリプトは Node 組込みだけで書く。bash 前提にしない (利用者に PowerShell の人がいる)。
- 手順書のコマンドは PowerShell と bash の両方で動く形か、`npm run` に畳む。

## 作業の入口

- `npm test` が緑であることを確かめてから始める。
- 直したら、雛形から `create` して `npm install && npm test` が通ることまで確かめる (`--kit-dep "git+file:///<このリポジトリの絶対パス>"`、コミット済みの内容だけが入る)。
- 課題は GitHub の Issues。
